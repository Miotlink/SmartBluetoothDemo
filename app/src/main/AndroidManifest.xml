<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.miotlink.android.bluetooth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:name=".application.SmartApplication"
        android:theme="@style/AppTheme">
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.ScanDeviceActivity"/>
        <activity
            android:screenOrientation="portrait"
            android:name=".ui.DeviceInfoActivity"/>

        <activity
            android:screenOrientation="portrait"
            android:name=".ui.DeviceControlActivity"/>

        <activity
            android:screenOrientation="portrait"
            android:name=".ui.DeviceSmartConfigActivity"/>
    </application>

</manifest>